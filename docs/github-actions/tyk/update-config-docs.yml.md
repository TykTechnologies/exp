# Update Config docs

```mermaid
stateDiagram-v2
    workflow : update-config-docs.yml - Update Config docs
    state workflow {
        sync: Tyk config docs
        state sync {
        }
    }
```
